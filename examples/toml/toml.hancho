#-------------------------------------------------------------------------------
# TOML parser example
from hancho import *

rules = include("{rules_path}/rules.hancho", build_config)

toml_lib = rules.c_library(
  "toml_parser.cpp",
  "toml_lib.a",
)

rules.c_test(
  "toml_test.cpp",
  "toml_test",
  libs = toml_lib,
  quiet = True
)
