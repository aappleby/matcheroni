#-------------------------------------------------------------------------------
# TOML parser example

rules = hancho.pop('rules')

lib = hancho(
  rules.cpp_lib,
  name    = "toml_parser",
  in_srcs = "toml_parser.cpp",
)

hancho(
  rules.cpp_test,
  name    = "toml_test",
  in_srcs = "toml_test.cpp",
  in_libs = lib,
)
