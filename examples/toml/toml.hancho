#-------------------------------------------------------------------------------
# TOML parser example

import rules

toml_lib = rules.c_library(
  name = "toml_lib",
  srcs = ["toml_parser.cpp"]
)

rules.run_test(
  name = "toml_test",
  srcs = ["toml_test.cpp"],
  deps = [toml_lib],
  quiet = True
)
