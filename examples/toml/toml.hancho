#-------------------------------------------------------------------------------
# TOML parser example

rules  = imports.rules
config = imports.config

toml_lib = rules.c_library(
  config,
  in_srcs = "toml_parser.cpp",
  out_lib = "toml_lib.a",
)

rules.c_test(
  config,
  in_srcs = "toml_test.cpp",
  out_bin = "toml_test",
  libs = toml_lib,
  quiet = True
)
