#-------------------------------------------------------------------------------
# TOML parser example

toml_parser = imports.rules.cpp_lib(
  name    = "toml_parser",
  in_srcs = "toml_parser.cpp",
)

imports.rules.c_test(
  name    = "toml_test",
  in_srcs = "toml_test.cpp",
  in_libs = toml_parser,
)
