#-------------------------------------------------------------------------------
# TOML parser example

toml_lib = hancho.rules.c_library(
  "toml_parser.cpp",
  "toml_lib.a",
)

hancho.rules.c_test(
  "toml_test.cpp",
  "toml_test",
  libs = toml_lib,
  quiet = True
)
