#-------------------------------------------------------------------------------
# TOML parser example

rules = load("config/rules.hancho")

toml_lib = rules.c_library(
  "toml_parser.cpp",
  "toml_lib.a",
)

rules.c_test(
  "toml_test.cpp",
  "toml_test",
  deps = toml_lib,
  quiet = True
)
