#-------------------------------------------------------------------------------
# TOML parser example

rules = build_config.include("{repo_path}/config/rules.hancho")

toml_lib = rules.c_library(
  "toml_parser.cpp",
  "toml_lib.a",
)

rules.c_test(
  "toml_test.cpp",
  "toml_test",
  libs = toml_lib,
  quiet = True
)
