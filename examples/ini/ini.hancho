#-------------------------------------------------------------------------------
# INI parser example

import hancho
rules = hancho.load("config/rules.hancho")

ini_parser_lib = rules.c_library(
  name = "ini_parser.a",
  srcs = "ini_parser.cpp",
)
